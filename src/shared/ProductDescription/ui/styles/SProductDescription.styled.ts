import styled from "styled-components";

const SProductDescription = styled.p`
  font-size: 16px;
  font-weight: 300;
  color: gray;

  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
`;

export default SProductDescription;
